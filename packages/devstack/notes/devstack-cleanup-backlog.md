# Devstack stage-A — verified dead-code / cleanup backlog

Source: adversarially-verified census (227 agents, 213 candidates) of `mh/devstack-stage-a` vs `main`. Every item below survived a skeptic that grepped the whole repo to refute it. Judged from code only.

**Totals:** 108 actionable items · ~520 confirmed-dead LOC · ~820 total prunable LOC.

## Action groups

### Unused exported API (barrel residue — re-export lines, type-only, zero downstream consumers)  _(~89 LOC, 25 items)_

- **ScopedMultimap (type re-export)** — `packages/devstack/src/substrate/runtime/scoped-registry/index.ts:24` — Delete the type-only re-export line; keep the interface in service.ts.  _(~1 LOC, low)_
- **MultimapEntry (type re-export)** — `packages/devstack/src/substrate/runtime/scoped-registry/index.ts:22` — Delete the type-only re-export line; keep the interface in service.ts.  _(~1 LOC, low)_
- **MultimapItem (type re-export)** — `packages/devstack/src/substrate/runtime/scoped-registry/index.ts:23` — Delete the type-only re-export line; keep the interface in service.ts.  _(~1 LOC, low)_
- **package/build.ts re-export block** — `packages/devstack/src/plugins/package/build.ts:18-28` — Delete the export-from '../sui/index.ts' block; KEEP line 29 export type { BuildOutput } and the internal imports on 12-14.  _(~11 LOC, low)_
- **extractTrailingJson (barrel re-exports)** — `packages/devstack/src/plugins/sui/index.ts + package/build.ts:20` — Remove the re-export from both barrels; keep the definition + internal use in move/index.ts.  _(~2 LOC, low)_
- **parseBuildOutput (barrel re-exports)** — `packages/devstack/src/plugins/sui/index.ts:405 + package/build.ts:21` — Delete both barrel re-export lines; keep the def + internal uses in move/index.ts.  _(~2 LOC, low)_
- **stripPinnedSections (barrel re-exports)** — `packages/devstack/src/plugins/sui/index.ts:406 + package/build.ts:22` — Delete both barrel re-export lines; keep the def + internal uses in move/index.ts.  _(~2 LOC, low)_
- **containerInnerScript (barrel re-exports)** — `packages/devstack/src/plugins/sui/index.ts:403 + package/build.ts:19` — Delete both barrel re-export lines; consumers resolve from move/index.ts directly.  _(~1 LOC, low)_
- **MoveBuildPhase (type re-exports)** — `packages/devstack/src/plugins/sui/index.ts:410 + package/build.ts:25` — Delete both dead type re-export lines; keep the def at move/index.ts:24.  _(~2 LOC, low)_
- **MoveBuildInput (interface + re-exports)** — `packages/devstack/src/plugins/sui/move/index.ts:122-126` — Delete the vestigial interface and its two re-exports (sui/index.ts:411, package/build.ts:26).  _(~7 LOC, low)_
- **MoveBuildOutput (interface + re-exports)** — `packages/devstack/src/plugins/sui/move/index.ts:128-132` — Delete the unused interface and its two re-exports (sui/index.ts:412, package/build.ts:27).  _(~7 LOC, low)_
- **SignAndDispatchResult / SignAndDispatchSigner / TransactionSignerSource (+ signAndDispatch re-export block)** — `packages/devstack/src/plugins/sui/exec/index.ts:428-433` — Delete the whole dead barrel re-export block; do NOT touch the source exports in sign-and-dispatch.ts (test imports them directly).  _(~6 LOC, low)_
- **makeDeepbookDeepFundingContribution (advanced-callers re-export)** — `packages/devstack/src/plugins/deepbook/index.ts:1077` — Delete the re-export line; keep the symbol + direct internal use.  _(~1 LOC, low)_
- **makeDeepbookDeepFundingStrategy (advanced-callers re-export)** — `packages/devstack/src/plugins/deepbook/index.ts:1078` — Delete the re-export line; keep the file-level import + def in faucet-strategy.ts.  _(~1 LOC, low)_
- **DeepbookDeepFundingStrategy / DeepbookDeepFundingStrategyOptions (type re-exports)** — `packages/devstack/src/plugins/deepbook/index.ts:1079-1080` — Delete the two type re-export lines; types stay live internally.  _(~2 LOC, low)_
- **DeepbookSpans (barrel re-export)** — `packages/devstack/src/plugins/deepbook/index.ts:1106` — Delete the re-export line; keep DeepbookSpans exported from spans.ts for in-plugin importers.  _(~1 LOC, low)_
- **WalFaucetRequest (type alias + re-export)** — `packages/devstack/src/plugins/walrus/faucet-strategy.ts:134-135 + walrus/index.ts:515` — Delete the type alias + its orphaned sub-barrel re-export; keep WalFaucetStrategy and walFaucetStrategyKey.  _(~3 LOC, low)_
- **WALRUS_ROUTER_PORT (barrel re-export)** — `packages/devstack/src/plugins/walrus/index.ts:523` — Delete only the re-export line; keep the live const in storage-nodes.ts.  _(~1 LOC, low)_
- **export type { ResolvedRoute } re-export** — `packages/devstack/src/orchestrators/boot.ts:829` — Delete the standalone re-export; keep the internal type import at line 94.  _(~1 LOC, low)_
- **gatherCaptureParticipants / CaptureInputs / GatheredCapture (barrel re-exports)** — `packages/devstack/src/orchestrators/snapshot/index.ts:20,23,24` — Delete the three barrel re-export lines; keep defs in capture.ts.  _(~3 LOC, low)_
- **RestoreInputs / WipeInputs / PruneInputs (barrel re-exports)** — `packages/devstack/src/orchestrators/snapshot/index.ts:32,43,48` — Delete the three *Inputs barrel re-export tokens; keep the local interfaces.  _(~3 LOC, low)_
- **requireIdentity / runRuntimeIdentityGuard / IdentityContribution / IdentityMissingLiveError / IdentityMissingSnapshotError (barrel re-exports)** — `packages/devstack/src/orchestrators/snapshot/index.ts:55,56,58,60,61` — Delete the 5 inert identity-guard re-export lines; keep defs in identity-guard.ts and the genuinely-consumed sibling re-exports.  _(~5 LOC, low)_
- **containerImagePath / IdentitySliceSchema / OpaqueContributionStateSchema / isValidSnapshotId / CapturedContainer / CapturedSubtree / ContributionDoc / IdentitySlice / OpaqueContributionState (barrel re-exports)** — `packages/devstack/src/orchestrators/snapshot/index.ts:68,72,73,80,83-87` — Delete the 9 inert descriptor re-export lines; keep the live SnapshotLayout/SnapshotMetadata/etc. block.  _(~9 LOC, low)_
- **mountAndRegisterDevWallet (+ Options/Result types)** — `packages/dev-wallet/src/index.ts:5-9` — Delete the root-barrel re-exports; all callers use the relative module path.  _(~5 LOC, low)_
- **EndpointSinks (interface)** — `packages/devstack/src/orchestrators/boot.ts:480-490` — Delete the named interface and drop the : EndpointSinks return annotation at boot.ts:516 (inferred shape is identical). [Note: a sibling verdict prefers only un-exporting; full delete is safe per the knip verdict since the inferred literal type is structurally identical.]  _(~11 LOC, low)_

### Unused exported API (whole symbols / interfaces with zero consumers)  _(~184 LOC, 13 items)_

- **defineScopedRegistry (+ defineMulti, multi:true overload apparatus)** — `packages/devstack/src/substrate/runtime/scoped-registry/service.ts:343-394` — Delete the dead dispatcher, defineMulti, both overload signatures + impl ternary, the barrel re-export (index.ts:19), and stale multi-mode prose. Collapse to defineSingle + defineScopedRefMap + makeScopedMultimap.  _(~50 LOC, low)_
- **manifest-bridge.ts (readEnvelope — whole module)** — `packages/devstack/src/orchestrators/codegen/manifest-bridge.ts` — Delete the whole file + the barrel line export * at codegen/index.ts:14; fix the dangling comment at service.ts:41.  _(~61 LOC, low)_
- **ReconcilePrecondition (interface + field)** — `packages/devstack/src/substrate/runtime/reconcile/spec.ts:230-236,272` — Delete the interface + the orphaned optional precondition? field; sweep the stale graph.ts:139 TODO.  _(~9 LOC, low)_
- **ReconcileLocks (interface + field)** — `packages/devstack/src/substrate/runtime/reconcile/spec.ts:238-242,278` — Delete the interface + locks? field + the spec.ts:19 doc bullet; prune graph.ts:139 TODO.  _(~6 LOC, low)_
- **ReconcileOwnership (interface + field)** — `packages/devstack/src/substrate/runtime/reconcile/spec.ts:244-249,279` — Delete the interface + ownership? field + the spec.ts:20 doc bullet + the graph.ts:139 TODO token.  _(~7 LOC, low)_
- **ProductionCodegenSeamInput (interface)** — `packages/devstack/src/orchestrators/boot.ts:327` — Delete the interface; inline its structural shape into resolveProductionCodegenOptions' parameter.  _(~14 LOC, low)_
- **ProductionPostAcquireOptions (interface)** — `packages/devstack/src/orchestrators/boot.ts:370` — Delete the interface; inline { readonly extras?: ManifestExtrasInput } onto the parameter.  _(~3 LOC, low)_
- **ManifestEndpointRegistry (interface)** — `packages/devstack/src/orchestrators/boot.ts:374` — Delete the named interface; inline its 2-property shape into the Context.Service call. Keep the Service + layer.  _(~4 LOC, low)_
- **SynthesisOverrides (interface)** — `packages/devstack/src/plugins/deepbook/synthesize.ts` — Delete the exported interface; annotate the single call site (synthesize.ts:106) with an inline object type.  _(~12 LOC, low)_
- **SynthesizedLocalDeepbook (interface)** — `packages/devstack/src/plugins/deepbook/synthesize.ts:58-64` — Delete the interface + the :SynthesizedLocalDeepbook return annotation (line 107); let TS infer the structural shape.  _(~7 LOC, low)_
- **GeneratedConfig (type)** — `packages/create-devstack-app/template/src/config.ts:10-13` — Delete the type export. [Whole config.ts module is also orphaned — flag as a separate full-module candidate.]  _(~4 LOC, low)_
- **isSharedLifecyclePruneGroup (dead export)** — `packages/devstack/src/orchestrators/lifecycle-prune/index.ts:312` — Drop the export; collapse the redundant in-file isSharedGroup alias (line 330) into the predicate. Keep the body (1 call site).  _(~2 LOC, low)_
- **DEFAULT_FORK_IMAGE_REF (const)** — `packages/devstack/src/plugins/sui/mode/fork.ts:64` — Delete the const (value is undefined) and inline the ?? undefined no-op at line 71; tidy the orphaned @link + doc block.  _(~5 LOC, low)_

### Dead-typed placeholder slots / over-general struct fields (never read)  _(~127 LOC, 18 items)_

- **cachePolicy axis (CachePolicy, cachePolicy(), reuseVerifiedPolicy(), preserveAllPolicy(), CacheDisposition, SnapshotsDisposition)** — `packages/devstack/src/substrate/runtime/reconcile/spec.ts` — Delete the whole inert cachePolicy axis (constructed at 6 sites, read at 0); drop the field + 6 call-site lines + the dead wipe.ts:213 const + the graph.ts:135-138 TODO.  _(~50 LOC, low)_
- **ReconcilePrecondition/ReconcileLocks/ReconcileOwnership fields** — `packages/devstack/src/substrate/runtime/reconcile/spec.ts` — Covered in 'whole symbols' group — the optional fields are the sole referencers and are themselves never read.  _(~0 LOC, low)_
- **plan _direction parameter** — `packages/devstack/src/substrate/runtime/reconcile/graph.ts:64` — Drop the unread 3rd parameter; update 4 call sites + test; sweep the stale comments (graph.ts:5,56-60).  _(~4 LOC, low)_
- **parentScope on ReconcileGraphDeps** — `packages/devstack/src/substrate/runtime/reconcile/graph.ts:93` — Delete the write-only field + doc comment + the teardown.ts:127 assignment; keep the local doSelectiveRestart param.  _(~7 LOC, low)_
- **SwapTreeOp.build discriminant** — `packages/devstack/src/substrate/runtime/reconcile/spec.ts:185` — Delete the write-only documentation discriminant + doc comment + the restore.ts:882 assignment.  _(~6 LOC, low)_
- **StageAndSwapPreservedPath.kind** — `packages/devstack/src/substrate/runtime/stage-and-swap/index.ts:69` — Delete the never-read 'file'|'directory' field + its 5 write sites in restore.ts + 3 test literals (fs.copy is kind-agnostic).  _(~1 LOC, low)_
- **BuildInputs.chainId** — `packages/devstack/src/plugins/sui/move/index.ts:69` — Delete the threaded-but-discarded field + 3 feeding object-literal props (build.ts:63, seal/deploy.ts:177, test:307). Do NOT touch the load-bearing package/build.ts:34 chainId.  _(~4 LOC, low)_
- **PackageRegistry.changes** — `packages/devstack/src/plugins/package/registry.ts:66,79` — Delete the write-only field + the wrapRefMap passthrough; drop the now-unused type Stream import.  _(~3 LOC, low)_
- **PackageRegistry.has** — `packages/devstack/src/plugins/package/registry.ts:64,77` — Delete the unused field + passthrough; optionally tidy the stale 16-19 comment.  _(~2 LOC, low)_
- **RecentBlob.deletable (+ asBool helper, BlobAcc plumbing)** — `apps/devstack-dashboard/src/lib/walrus.ts` — Delete the dead field + its accumulator/fold plumbing (139,172,179) + the now-orphaned asBool helper (67).  _(~6 LOC, low)_
- **RecentBlob.sender (+ GraphQL selection + plumbing)** — `apps/devstack-dashboard/src/lib/walrus.ts` — Delete the unread field + BlobAcc.sender (142), init (175), assignment (190), and the sender { address } GraphQL selection (106-108).  _(~8 LOC, low)_
- **AddressTransaction.sender (+ mapper + 3 query selections)** — `apps/devstack-dashboard/src/lib/sui-graphql.ts:96` — Delete the never-read field, the mapper line (207), and the sender { address } sub-selection from all three query docs.  _(~11 LOC, low)_
- **OwnedObjectView.digest** — `apps/devstack-dashboard/src/lib/explorerTypes.ts:178` — Delete the field + the dead write at chain.ts:347 (digest: o.digest).  _(~2 LOC, low)_
- **FundResult.digest (cross-layer wire field, always null)** — `apps/devstack-dashboard/src/lib/api.ts:380` — Coordinated wire-contract removal: client field + FundDoc selection (458) + server FundResultShape.digest + resolver + schema.graphql + DashboardFundResult.digest + the 14 digest:null literals; regenerate graphql-env.d.ts. Keep MintResult.digest.  _(~3 LOC, medium)_
- **rowStatusCounts (buildStatusPayload)** — `packages/devstack/src/surfaces/cli/commands/status.ts:50-56` — Delete the unread nested-counts block + the vm = deriveStackViewModel line (40) + its import (15). Keep flat counts. [Cosmetic breaking change to published status JSON; zero in-repo consumers.]  _(~9 LOC, low)_
- **autoApprove (DevstackVitePluginOptions field)** — `packages/devstack/src/build-integrations/vite/index.ts:80-84` — Delete the field + JSDoc; simplify line 307 to autoApproveFromEnv(env). Env DEVSTACK_AUTO_APPROVE remains the sole control.  _(~5 LOC, low)_
- **PluginModule.id (template)** — `packages/create-devstack-app/template/src/devstack/contribution.ts:43` — Delete the write-only field + 3 id: literals (seal.ts:12, walrus.ts:10, deepbook.ts:10) in one change; trim the doc-comment. Template tree is typechecked.  _(~4 LOC, low)_
- **deepbookMoveSource union arms 'deepbookv3/token' and 'deepbookv3/dusdc'** — `packages/devstack/src/plugins/deepbook/bootstrap-assets/index.ts` — Delete the two never-passed union arms; narrow to the two values the wrappers supply.  _(~2 LOC, low)_

### Over-general / unread return values & dead branches  _(~12 LOC, 4 items)_

- **makePluginCtx (pluginKey | null = null default + null ternary)** — `packages/devstack/src/substrate/runtime/supervisor/acquire-node.ts` — Tighten to pluginKey: PluginKey; collapse the unreachable null branch in ContributionBufferSealedError. Caller always passes non-null.  _(~2 LOC, low)_
- **publishHookFailure (pluginKey = null default)** — `packages/devstack/src/substrate/runtime/supervisor/background-tasks.ts:220` — Drop the unexercised = null default (sole caller passes explicitly); keep the function body.  _(~1 LOC, low)_
- **GraphPlan return of reconcileGraph + double-ordering** — `packages/devstack/src/substrate/runtime/reconcile/graph.ts:115` — In-place refactor (NOT a delete): change reconcileGraph return to Effect<void>, drop the discarded return built; optionally narrow planRestart. Keep GraphPlan + plan/planExcluding (live).  _(~7 LOC, low)_
- **DEFAULT_DEEP_SUI_POOL.stablePool** — `packages/devstack/src/plugins/deepbook/synthesize.ts:55,147` — Optional micro-dedup: drop the field + the thread (re-supplied identically by index.ts:387 ?? false). Bundle into backlog tidy.  _(~2 LOC, low)_

### No-op indirection / inlinable single-prod-caller (de-export or fold)  _(~53 LOC, 12 items)_

- **StrategyFor<K> (substrate plugin-ctx alias)** — `packages/devstack/src/substrate/plugin-ctx.ts:85` — Inline the no-op alias at the one call site (line 194 -> Effect.Effect<unknown, StrategyNotFoundError>) and delete the alias + 15-line doc. Keep ctx.requires. Do NOT touch the 2-param StrategyFor in contracts/strategy-contributor.ts.  _(~12 LOC, low)_
- **formatCause (supervisor local clone)** — `packages/devstack/src/substrate/runtime/supervisor/acquire-node.ts:201-218` — Dedup: delete the local helper, repoint line 313 to formatValue (already barrel-imported). formatValue is a behavioral superset.  _(~17 LOC, low)_
- **runInjectedCommandHandler** — `packages/devstack/src/substrate/runtime/supervisor/background-tasks.ts` — Inline the wrapper at command-loop.ts:311 via Effect.asVoid(runInjectedCommandHandlerExit(...)) (already imported); delete the export + import line.  _(~4 LOC, low)_
- **productionRouterProfile** — `packages/devstack/src/orchestrators/boot.ts:419-420` — Inline the passthrough at boot.ts:438 to makeDefaultRouterProfile(); delete the export; retarget 2 test files to import makeDefaultRouterProfile from the router barrel.  _(~3 LOC, low)_
- **ProductionRouterOptions.profile / .image** — `packages/devstack/src/orchestrators/boot.ts:366-367` — Delete both unset fields; inline the defaults in layerProductionOrchestrators (438, 451). Keep codegen/disabled.  _(~3 LOC, low)_
- **SynthesizedLocalDeepbook.deepTreasuryIdKey** — `packages/devstack/src/plugins/deepbook/synthesize.ts:63,199` — Delete the field; rewrite consumer index.ts:543 to opts.deepTreasuryIdKey ?? 'deepTreasuryId' (or module const). Behavior-preserving.  _(~2 LOC, low)_
- **containerInnerScriptOneShot (export keyword)** — `packages/devstack/src/plugins/sui/move/index.ts:207` — Covered by the duplicate-collapse item below; the export is single-in-file-caller residue.  _(~1 LOC, low)_
- **stageLocalMoveDeps (export keyword)** — `packages/devstack/src/plugins/sui/move/index.ts:527` — Drop the export keyword (sole caller is in-file buildViaOneShot); keep copyLocalMoveDeps exported.  _(~1 LOC, low)_
- **containerInnerScriptOneShot / SuiExecuteClient / SerializedTxBuilder / ExecutedObjectChange / SuiExecuteResult (over-exported exec internals)** — `packages/devstack/src/plugins/sui/exec/index.ts` — Drop the export keyword (or inline) on each — all are file-local-only; published .d.ts is unaffected (verified isolatedDeclarations clean).  _(~4 LOC, low)_
- **walrusGraphqlUrl (bare value re-export)** — `apps/devstack-dashboard/src/lib/walrus.ts:40` — Atomic repoint: delete the alias, import suiGraphqlUrl from sui-graphql.ts in Walrus.tsx, change line 160. Matches sibling panels.  _(~1 LOC, low)_
- **over-exported tar/reader internals + boot/vite/fork/seal/local const exports** — `packages/devstack/src/substrate/runtime/tar/reader.ts; build-integrations/vite/index.ts; sui/mode/{fork,local}.ts; seal/keygen.ts` — Drop the export keyword (un-export) on the file-local-only symbols: 9 tar reader helpers; DEFAULT_DEV_EXTRAS_ALIAS/DEFAULT_GENERATED_ALIAS; FORK_IMAGE_ENV_VAR, DEFAULT_LOCAL/FORK_READY_TIMEOUT, FORK_VALIDATOR_STOP_GRACE_SECONDS; SEAL_KEYGEN_ENTRYPOINT/ARGS, CONTAINER_SCRUB_AWK_SCRIPT; HealthPolicy/HealthState/StackViewModel/DashboardSummary/DisplayCells (TUI display-derivation). Bodies stay; 0 LOC net but shrinks public surface. Do last — several touch tests asserting on the export.  _(~0 LOC, low)_
- **DEFAULT_SUI_VERSION (sui/mode alias)** — `packages/devstack/src/plugins/sui/mode/local.ts:108-112` — Delete the re-alias + doc; repoint line 300 to DEFAULT_SUI_CLI_VERSION (already imported). Do NOT touch the unrelated walrus DEFAULT_SUI_VERSION.  _(~5 LOC, low)_

### Duplication (dedup against an existing canonical)  _(~82 LOC, 9 items)_

- **ReconcileLabelFailer (duplicate of ReconcileFsFailer)** — `packages/devstack/src/substrate/runtime/reconcile/label.ts` — Delete the byte-identical alias + doc; import ReconcileFsFailer from spec.ts and retype the 3 onContainers/Networks/VolumesError fields.  _(~3 LOC, low)_
- **containerInnerScriptOneShot (clone of containerInnerScript)** — `packages/devstack/src/plugins/sui/move/index.ts` — Delete the clone (~198-237); point buildViaOneShot (line 551) at containerInnerScript(pkgName); fold the one-shot EACCES rationale into the kept comment.  _(~30 LOC, low)_
- **faucetCauseDetail (no-op alias)** — `packages/devstack/src/plugins/dashboard/domain.ts:378` — Inline causeDetail at line 950; delete the alias.  _(~1 LOC, low)_
- **fundingCauseDetail (no-op alias)** — `packages/devstack/src/plugins/dashboard/domain.ts:379` — Inline causeDetail at line 1026; delete the alias.  _(~1 LOC, low)_
- **SUI_FULL_COIN_TYPE (dashboard local copy)** — `packages/devstack/src/plugins/dashboard/domain.ts:318-322` — Delete the local const + doc; import SUI_FULL_COIN_TYPE from ../account/index.ts. 3 in-file consumers bind to the SSOT.  _(~4 LOC, low)_
- **'0x2::sui::SUI' inline literal** — `packages/devstack/src/plugins/walrus/wal-swap.ts:110` — Replace the literal with the already-imported SUI_FULL_COIN_TYPE constant. Net 0 LOC (quality dedup).  _(~0 LOC, low)_
- **UseDevWalletOptions.createInitialAccount (redeclared field)** — `packages/dev-wallet/src/react/useDevWallet.ts:13-14` — Delete the redeclaration (inherited from MountAndRegisterDevWalletOptions). Move the 'defaults to true' note into the hook JSDoc to avoid a hover regression.  _(~2 LOC, low)_
- **ADDRESS_TXS_SENT / ADDRESS_TXS_RECEIVED / OBJECT_TXS_AFFECTED (3 cloned GraphQL docs)** — `apps/devstack-dashboard/src/lib/sui-graphql.ts:114-172` — Refactor (NOT delete): collapse to one TXS_BY_FILTER doc with a $filter: TransactionFilter! variable; map direction -> {sentAddress|affectedAddress|affectedObject}. Behavior-equivalent; feature stays live.  _(~33 LOC, low)_
- **useAddressBalances (one-line delegate of useBalances)** — `apps/devstack-dashboard/src/lib/useChain.ts:206-213` — Inline-and-remove: migrate the 2 call sites (AddressDetail.tsx:66, ObjectDetail.tsx:102) to useBalances, drop the comment mention, delete the delegate.  _(~8 LOC, low)_

### Test-only exports (de-export, repoint or drop test)  _(~101 LOC, 10 items)_

- **FormatterRegistryShape.tags (diagnostic field)** — `packages/devstack/src/substrate/runtime/supervisor/formatter-registry.ts:45-46,106,108` — Delete the field + impl + service slot + the noop entry (acquire-node.ts:125); rewrite/remove the scope-close test to observe reaping through snapshot.  _(~6 LOC, low)_
- **endpointsSummaryForRow** — `packages/devstack/src/surfaces/tui/display-derivation.ts:667-674` — Delete the export + its lone test (import line 34, assertions at 427 & 453). Production renders via visibleEndpointsForRow + endpointLine.  _(~22 LOC, low)_
- **CoinRegistry.bySymbol** — `packages/devstack/src/plugins/coin/registry.ts:79,92-100` — Delete the field + impl + the orphaned test blocks (registry.test.ts:57-122); trim the stale 'five lookup shapes' comments.  _(~12 LOC, low)_
- **SuiExecuteClient (alias)** — `packages/devstack/src/plugins/sui/exec/index.ts:67-71,225` — Inline to ClientWithCoreApi at line 225 (drop the alias + doc); have the test cast mocks as ClientWithCoreApi.  _(~5 LOC, low)_
- **selectOwnedCoinsForBalance / setExplicitSeedGasPayment (export keyword)** — `packages/devstack/src/plugins/deepbook/deploy.ts` — Drop the export keyword (both have live in-module callers); delete or repoint the seed-gas.test.ts imports through the public seedDeepbookPools surface.  _(~3 LOC, low)_
- **MintTransactionSigner / MintSignAndExecuteResult (export keyword)** — `packages/devstack/src/plugins/coin/mint.ts:62,78` — Drop the export keyword on both base types (file-local only).  _(~0 LOC, low)_
- **TUI display-derivation single-in-file-caller exports (roleLabelColor, sectionForRow, ownerForRow, endpointsForRow)** — `packages/devstack/src/surfaces/tui/display-derivation.ts` — Drop the export keyword (functions are live in-file via deriveDisplayCells); delete or fold the direct unit-test assertions. Do last — touches tests; preserve the endpointsForRow prefix-collision assertion if kept.  _(~4 LOC, low)_
- **snapshotPhaseLabel / snapshotPhaseMeansPaused (producerless running-progress UI)** — `packages/devstack/src/surfaces/tui/dashboard.tsx:106-138` — Remove together with the captureStarted/captureProgress chain (see staged plan); these helpers are reachable only via the producerless 'running' case.  _(~35 LOC, medium)_
- **routable (authoring helper)** — `packages/devstack/src/api/define-capabilities.ts:25-34` — Delete the helper + RoutableDeclInput type + the barrel line (index.ts:33) + the self-referential routable.test.ts block. MEDIUM only because it is a published barrel export with no in-repo consumer.  _(~10 LOC, medium)_
- **strategyContributor (authoring helper)** — `packages/devstack/src/api/define-capabilities.ts:62-64` — Delete the helper + barrel line (index.ts:35) + the consuming test block. Prod uses inline literals. [Owner may keep for public-API symmetry — see safety note.]  _(~4 LOC, low)_

### Stale comments / deletion residue (comment-only edits)  _(~43 LOC, 20 items)_

- **CapabilitySinks/capability-sinks residue (scoped-registry header)** — `packages/devstack/src/substrate/runtime/scoped-registry/service.ts:7,35` — Trim 'and (historically) CapabilitySinks' and the stale bug-attribution; keep the seq-tagged design rationale. Do NOT touch CHANGELOG.  _(~2 LOC, low)_
- **state-store durability contrast** — `packages/devstack/src/primitives/cache.ts:4-5` — Drop the '; state-store entries are durable' clause; the subsystem is deleted.  _(~1 LOC, low)_
- **deleted lock.ts path references** — `packages/devstack/src/substrate/runtime/cross-process/live-clock.ts:49 + observability/logger.ts:18` — Repoint both stale doc comments at the successor cross-process/stack-lock.ts (prose is accurate, only the path is wrong).  _(~0 LOC, low)_
- **dispatchFailed 'capability sink' wording + span name + errorContributionSink parentheticals** — `packages/devstack/src/substrate/runtime/supervisor/acquire-node.ts:271,308,321 + start-supervisor.ts:329` — Rename the live span label to dispatchBufferedContributions; reword 'capability sink' -> 'routing/contribution sink' (update the 5 pinning test fixtures); strip the '(was errorContributionSink)' parentheticals. Spans/strings stay live.  _(~2 LOC, low)_
- **noopContributionDispatcher OrchestratorSinks clause** — `packages/devstack/src/substrate/runtime/supervisor/contribution-dispatcher.ts:85-86` — Delete only the '(matching the old empty OrchestratorSinks default)' clause; keep the export + accurate first sentence.  _(~1 LOC, low)_
- **makePluginCtx resolveCapabilities/capabilities-closure clause** — `packages/devstack/src/substrate/runtime/supervisor/acquire-node.ts:134-135` — Remove the stale '(the legacy capabilities closure + resolveCapabilities harvest are gone)' parenthetical.  _(~1 LOC, low)_
- **end-to-end-run orphaned JSDoc block** — `packages/devstack/src/orchestrators/boot.ts:294-302` — Delete the 9-line dangling JSDoc (documents a deleted fn + non-existent opts.loggerLayer).  _(~9 LOC, low)_
- **ProductionCodegenOptions.extrasDir garbled JSDoc** — `packages/devstack/src/orchestrators/boot.ts:314-319` — Rewrite the mid-edit-spliced JSDoc to an accurate single sentence; keep the field.  _(~3 LOC, low)_
- **boot.ts merged-module provenance header** — `packages/devstack/src/orchestrators/boot.ts:3-8` — Prune the 'Merges the former run.ts, runtime-composition.ts, built-in-plugin-layers.ts' sentence (all deleted); keep L10-23 current-responsibility prose. Do NOT touch L304/L791 dividers.  _(~4 LOC, low)_
- **artifact-publisher / sui-move-build doc-path residue** — `packages/devstack STYLE_GUIDE.md:122,285 + ARCHITECTURE.md:222 + codegen/bindings.ts:382` — Scrub stale references to the deleted substrate/runtime/artifact-publisher/ path and fix bindings.ts:382 'sui-move-build/index.ts' -> 'plugins/sui/move/index.ts'.  _(~0 LOC, low)_
- **exec span 'substrate.sui-execute' namespace label** — `packages/devstack/src/plugins/sui/exec/index.ts:390` — Rename to 'devstack.plugin.sui.execute' (the 'substrate.' prefix is orphaned post-hoist; mislabels service in dashboard).  _(~0 LOC, low)_
- **defineSimpleConstExport header comment** — `packages/devstack/src/plugins/internal/codegen-helpers.ts:3-4` — Correct the 3-of-5 consumer list to include seal + deepbook (or drop the filename list).  _(~0 LOC, low)_
- **PackageRegistry byPublisher/byPackageId speculation** — `packages/devstack/src/plugins/package/registry.ts:16-19,59-60` — Delete the unrealized forward-looking lookup speculation; keep the architecture/SSOT prose.  _(~3 LOC, low)_
- **next-steps log 'publish hello'** — `packages/create-devstack-app/src/index.ts:145` — Replace 'publish hello' with 'publish move packages' / 'publish counter' (hello package deleted; scaffold publishes counter/vault).  _(~1 LOC, low)_
- **PluginContext 'today plugins need nothing from core' JSDoc (template)** — `packages/create-devstack-app/template/src/devstack/contribution.ts:31-33` — Rewrite the false clause (seal.ts already reads ctx.here today); ships verbatim to scaffolds.  _(~2 LOC, low)_
- **built-in-plugin-layers.test.ts cross-reference clause** — `packages/devstack/test/plugins/package/coin-discovery.test.ts:15-17` — Delete the stale 'mirrors ... built-in-plugin-layers.test.ts (still covers the now-dead sink path)' clause (file + path both deleted).  _(~3 LOC, low)_
- **snapshot-reservation co-consumer clause** — `packages/devstack/test/substrate/runtime/cross-process/reclaim-stale-file.test.ts:1-4` — Drop the deleted snapshot-reservation.ts co-consumer from the 'shared by' header; stack-lock.ts is the sole remaining consumer.  _(~1 LOC, low)_
- **capability-decl redesign-opportunity paragraph** — `packages/devstack/src/plugins/account/registry.ts:15-19` — Delete the paragraph (references deleted capability-decl.ts + nonexistent publishAccount); keep header + per-symbol docs.  _(~5 LOC, low)_
- **CapabilityDecl second-closure migration comment** — `packages/devstack/test/helpers/test-plugin-ctx.ts:5-6` — Trim the historical from-to clause narrating the deleted CapabilityDecl type; keep the present-tense ctx-verb description.  _(~2 LOC, low)_
- **GUARD-B 'no plugin authors through ctx yet' clause** — `packages/devstack/test/substrate/strategy-bus-guard.test.ts:16-18` — Fix the false framing (plugins DO author through ctx now; scan passes because keys are computed via helpers, not string literals). KEEP the test + the line-10 CapabilitySinks rationale.  _(~3 LOC, low)_

## Staged plan (synthesis order)

1. STAGE 1 — Pure barrel-residue & type-only re-export deletions (no body changes, independently typecheckable). Delete the dead re-export lines in scoped-registry/index.ts (ScopedMultimap, MultimapEntry, MultimapItem), sui/index.ts + package/build.ts (extractTrailingJson, parseBuildOutput, stripPinnedSections, containerInnerScript, MoveBuildPhase, MoveBuildInput, MoveBuildOutput re-exports + the whole package/build.ts:18-28 block keeping line 29), the SignAndDispatch* block (exec/index.ts:428-433), deepbook advanced-callers re-exports (1077-1080,1106), WalFaucetRequest (faucet-strategy.ts + walrus/index.ts:515), WALRUS_ROUTER_PORT (walrus/index.ts:523), boot.ts:829 ResolvedRoute, snapshot/index.ts inert re-exports (gather*/RestoreInputs/WipeInputs/PruneInputs/identity-guard 5/descriptor 9), and dev-wallet root-barrel mountAndRegisterDevWallet block. Typecheck once.
2. STAGE 2 — Whole dead exported symbols & inert placeholder fields (zero readers; no test repoint). Delete manifest-bridge.ts + its barrel line; the defineScopedRegistry/defineMulti multi apparatus; reconcile spec.ts placeholders (ReconcilePrecondition/ReconcileLocks/ReconcileOwnership + fields + graph.ts:139 TODO); the cachePolicy axis (6 call sites + wipe.ts:213 + graph.ts:135-138); boot.ts interfaces (ProductionCodegenSeamInput, ProductionPostAcquireOptions, ManifestEndpointRegistry, EndpointSinks); deepbook synthesize interfaces (SynthesisOverrides, SynthesizedLocalDeepbook); GeneratedConfig; the unread struct fields (parentScope, SwapTreeOp.build, StageAndSwapPreservedPath.kind, BuildInputs.chainId, PackageRegistry.changes/has, deepbookMoveSource union arms). Typecheck once.
3. STAGE 3 — Dashboard-app & published-field dead data paths (app tree, independently typecheckable). Remove RecentBlob.deletable/.sender + asBool + plumbing, AddressTransaction.sender + GraphQL selections, OwnedObjectView.digest + chain.ts:347, rowStatusCounts, autoApprove vite field, PluginModule.id template field. Then the coordinated FundResult.digest wire-contract removal (client+server+schema+regenerate graphql-env.d.ts). Typecheck the dashboard + devstack packages.
4. STAGE 4 — De-export / inline single-in-file-caller residue (body stays, no test touch). Drop the export keyword / inline on: StrategyFor<K> alias, formatCause dedup -> formatValue, runInjectedCommandHandler inline, productionRouterProfile inline + 2 test repoints, ProductionRouterOptions.profile/.image, DEFAULT_FORK_IMAGE_REF, isSharedLifecyclePruneGroup, the tar/reader 9 helpers, vite/fork/local/seal/keygen const un-exports, exec internals (SuiExecuteClient/SerializedTxBuilder/ExecutedObjectChange/SuiExecuteResult), stageLocalMoveDeps, walrusGraphqlUrl repoint, DEFAULT_SUI_VERSION alias, TUI display-derivation type un-exports. Typecheck (and dashboard build where touched).
5. STAGE 5 — Duplicate collapses requiring a structural edit. ReconcileLabelFailer -> ReconcileFsFailer; containerInnerScriptOneShot clone -> containerInnerScript; faucet/fundingCauseDetail + SUI_FULL_COIN_TYPE + wal-swap literal dedups; UseDevWalletOptions.createInitialAccount; the 3-clone GraphQL TXS_BY_FILTER consolidation; useAddressBalances inline + 2 call-site migration. Typecheck per touched package.
6. STAGE 6 — Test-coupled removals (need a test repoint or behavioral re-observation; do last). FormatterRegistryShape.tags (rewrite scope-close test through snapshot); endpointsSummaryForRow + its test; CoinRegistry.bySymbol + orphaned test blocks; SuiExecuteClient/seed-gas/coin-mint test-only export drops; routable + strategyContributor authoring helpers + their contract tests; TUI display-derivation single-caller export drops + fold/delete their direct assertions; the producerless captureStarted/captureProgress running-progress chain removal (snapshotPhaseLabel/snapshotPhaseMeansPaused + events + all consumer switch cases + test fixtures) as one atomic producer-and-consumer change. Run the full devstack + dashboard typecheck and targeted test suites after.
7. STAGE 7 — Comment-only sweep (no compile impact; batch last). Apply all 'Stale comments / deletion residue' edits: scoped-registry header, cache.ts state-store clause, lock.ts path repoints, capability-sink wording + span rename + parentheticals, OrchestratorSinks/resolveCapabilities clauses, boot.ts orphaned JSDoc + provenance header + extrasDir JSDoc, doc-path residue (STYLE_GUIDE/ARCHITECTURE/bindings.ts:382), exec span namespace label, defineSimpleConstExport list, PackageRegistry speculation, 'publish hello', template PluginContext JSDoc, coin-discovery + reclaim-stale-file + account/registry + test-plugin-ctx + GUARD-B clauses.

## ⚠ Safety flags (do NOT auto-cut — review)

- FEATURE-REMOVED (medium) — snapshot integrity subsystem (writeArtifactIntegrity/verifyArtifactIntegrity/IntegrityFileSchema/integrity.json): already cleanly deleted (no residue), but the replacement (atomic tree publish + Docker tag check + path-traversal guard) does NOT detect silent per-byte content corruption/tampering of published artifact bytes that the per-file SHA-256 manifest specifically caught. Owner must confirm the threat model; re-introduce a content/manifest digest if restored-artifact integrity is in-scope.
- FEATURE-REMOVED (medium) — snapshot crash-recovery pending-marker subsystem (pending-marker.ts / recover-pending.ts / state-document.ts): cleanly deleted, but a HARD kill (SIGKILL/power loss) mid promoteStagedImages now leaves K-of-N images promoted with no breadcrumb; next boot's decideRunAction returns 'fresh' and docker-runs a target image with no backing image. Old scanner auto-completed promotions (with a digest fallback). Owner must confirm long-running/supervised deployments tolerate a manual restore re-run, or add a minimal interrupted-restore detector / hard-kill e2e.
- FEATURE-REMOVED (low) — clearStaleRestoreMarker / restore-marker-tolerance.ts: backward-compat shim deletion; the module's call sites are already removed in the working tree. Operators upgrading from a pre-D2 binary that crashed mid image-promotion will leave an inert snapshot.restore-pending.json (harmless clutter). Note in release notes if a clean migration matters.
- FEATURE-REMOVED (medium) — snapshot.captureProgress / captureStarted running-progress TUI: no production producer exists (only a test fixture + a stale dist artifact emit them), so the UI never renders. Removal must be ATOMIC across events.ts + every consumer switch (app.tsx, event-log.ts, plain-renderer.ts, projection/update.ts, event-time.ts, dashboard.tsx) + the test fixture — deleting dashboard.tsx:140 in isolation breaks SnapshotStatusLine exhaustiveness. Confirm no operator-facing progress signal is desired.
- FEATURE-REMOVED (low) — Seal dashboard 'Policies' panel + ProbeState 'unhealthy': Policies panel removal is harmless (empty placeholder). But the probe change collapsed a RED 'unhealthy' (non-2xx /health, e.g. 502/503 from a dead daemon behind a proxy) into a YELLOW 'unreachable' with softened copy. If a distinct daemon-down indicator matters, restore a red state for the in-CORS non-2xx branch.
- NEEDS-HUMAN (medium) — assertNoDanglingRefs (create-devstack-app/compose.ts): a deliberate defensive scaffold-time guard with a dedicated asserting test; matches the repo's load-bearing-invariant pattern. Do NOT auto-delete. Removal (~50 LOC incl. walkFiles/TEXT_EXTS/moduleKey/escapeRegExp + test + README note) is only valid if the owner accepts the regenerate-not-splice design makes the backstop redundant.
- PUBLIC-API NOTE (low) — several action items touch published-package surface (ctx.persist/requires/fail on the public PluginCtx type; routable/strategyContributor/snapshotable authoring helpers; DevstackVitePluginOptions fields; status JSON rowStatusCounts; FundResult.digest GraphQL field). All have zero in-repo consumers, but if external out-of-repo SDK/plugin authors are a supported audience, gate the removals behind a semver-major bump or deprecation cycle.
- OWNER-DECISION (low) — define-capabilities.ts authoring-helper set: snapshotable/routable/strategyContributor are test-only public exports while codegenable/projection are live. Decide as a SET whether external plugin authors should build decls inline (drop all three test-only helpers symmetrically) or the public authoring surface is retained (keep all five). Do not delete asymmetrically.

## Feature-removed verdicts (behavioral gaps to confirm intended)

- **acquireExclusive + oneShotSweep + timeoutMillis===0 branch** — `packages/devstack/src/substrate/runtime/cross-process/stack-lock.ts` — The candidate symbols acquireExclusive/oneShotSweep/toHolder/ownBody/mapHeld/mapIo and the timeoutMillis===0 one-shot branch do NOT exist anywhere at HEAD — `git grep` against HEAD and an rg sweep both return zero hits. The proposed cleanup (inline the generic into acquireStackLock, drop oneShotSweep + the one-shot branch) was ALREADY committed at HEAD (commit 8aba3389); the working tree is clean. The surviving concrete acquireStackLock (lines 160-276) is heavily live with 10+ production call sites and must stay. → No action — this candidate is already resolved in the committed HEAD, not just the working tree as the finder hedged. There is no acquireExclusive wrapper, no oneShotSweep, and no timeoutMillis===0 branch left to remove (estLocSaved = 0). Do NOT touch acquireStackLock: it is the live, multi-consumer cross-process lock primitive. The only residual is a harmless doc-comment mention of a 'one-shot orphan check' in liveness.ts:152, which is unrelated stale prose and out of this candidate's scope.
- **clearStaleRestoreMarker + RESTORE_PENDING_FILE_NAME (whole module restore-marker-tolerance.ts)** — `packages/devstack/src/cli/wirings/restore-marker-tolerance.ts` — Finder's 'ZERO consumers' rationale is WRONG at HEAD: apply.ts and up.ts each import+call clearStaleRestoreMarker before plugin acquire (confirmed in committed src and dist/*.mjs). BUT the working tree already removes both call sites cleanly (git diff shows the import+call deleted, plus the now-unused FileSystem import dropped from apply.ts), so the module is genuinely orphaned in the working-tree state. The function only unlinks a stale snapshot.restore-pending.json that a pre-D2 binary could leave; current src has no writer/reader/scanner of that marker (restore.ts documents 'NO crash-recovery marker is written'), so removal causes no behavior gap for any stack the current code produces — it drops a backward-compat shim whose own comment says a leftover file 'would only be inert clutter'. The two test hits are literal-string negative assertions, not module imports, and remain valid after deletion. → Delete the module and keep the already-staged call-site removals in apply.ts and up.ts as a single coordinated change. The deletion is a backward-compat shim removal, not a safety regression. Optional caveat for the human: operators upgrading from a pre-D2 binary that crashed mid image-promotion will now leave an inert snapshot.restore-pending.json on disk instead of having it auto-cleaned — harmless clutter, but if a clean migration is desired, note it in release notes. After deleting, run a typecheck to confirm no orphaned FileSystem/fs references remain (up.ts still legitimately uses fs at line 510; apply.ts correctly dropped its FileSystem import).
- **snapshot integrity subsystem (writeArtifactIntegrity / verifyArtifactIntegrity / IntegrityFileSchema / integrity.json)** — `` — Cannot refute the removal-cleanup itself (zero residual references to any integrity symbol in src/test on this branch — fully clean, nothing left to delete), but I confirm the finder's safety concern: on main, verifyArtifactIntegrity ran on EVERY restore (restore.ts:242) and writeArtifactIntegrity ran on EVERY capture (capture.ts:811) — live production behavior, not dead code. The replacement (atomic tree publish + verifyImageBundleTags Docker save-manifest TAG check at restore.ts:449 + isSafeSnapshotRelativePath tar path-validation at restore.ts:302) covers half-written trees, tag mismatch, and path traversal — but NOT silent per-byte content corruption/tampering of published artifact bytes, which the per-file SHA-256 manifest (integrity.json) specifically detected. Real safety-coverage delta with no equivalent re-hash; restore.ts:748 comment explicitly acknowledges integrity re-hash was dropped. → Keep flagged for human review — do NOT treat as a removable/cleanup item. The symbol is already fully and cleanly deleted (no dangling refs), so there is no further deletion to perform. The owner must confirm the threat model: atomic-publish guarantees a complete tree but does not detect post-publish on-disk byte corruption/tampering of artifact files or image layers (the Docker manifest check verifies tags, not layer content). If corruption/tamper detection of restored artifacts is in-scope, re-introduce a content hash (or at minimum a manifest-level digest over the image bundle blobs); if explicitly out-of-scope (single-host, trusted, ephemeral dev artifacts), document that decision and the removal stands.
- **snapshot crash-recovery pending-marker subsystem (pending-marker.ts / recover-pending.ts / state-document.ts)** — `packages/devstack/src/orchestrators/snapshot/restore.ts` — No dangling consumer: barrel index.ts is clean, the old boot-time scanner calls at cli/wirings/apply.ts:151 and up.ts:545 are both removed, build is internally consistent. BUT a real safety automation is gone: a HARD process kill (SIGKILL/power loss) mid-promoteStagedImages leaves K-of-N images promoted and N-K staging refs orphaned; Effect.uninterruptible does NOT survive SIGKILL and the scope finalizer does NOT run, so the swap has landed but on next boot nothing re-runs restore (the recoverPendingRestore hook was deleted) — decideRunAction returns {kind:'fresh'} and docker-runs a target image name with no backing image, which fails. The deleted scanner completed those promotions automatically (with a digest fallback even after `docker system prune`); the new design needs a MANUAL re-run with no breadcrumb. Files are dead-as-code but the capability reduction is real. → Keep the three files DELETED (they are confirmed dead — zero prod/test consumers, clean barrel, consistent build), but DO NOT rubber-stamp the capability loss. Flag for human review: confirm the owner accepts that a hard-kill (not just clean-failure) mid image-promotion now requires a manual `restore` re-run with no on-disk breadcrumb, where the old subsystem auto-recovered at boot with a digest fallback. If the supervised/long-running deployment must tolerate power loss mid-restore, either (a) re-introduce a minimal boot-time interrupted-restore detector, or (b) add a hard-kill-path e2e proving decideRunAction recreate-from-fresh genuinely recovers an interrupted mid-promotion before closing this out. estLocSaved is 0 because the deletion already happened on this branch; this verifier only blocks treating it as zero-risk.
- **ProbeState 'unhealthy' + Policies panel (apps/devstack-dashboard/src/panels/plugins/Seal.tsx)** — `apps/devstack-dashboard/src/panels/plugins/Seal.tsx` — Nothing breaks: grepHint title=\"Policies\" has zero hits repo-wide (excl. notes/md); ProbeState/PROBE_TOKEN/PROBE_LABEL/'unhealthy' have no consumer outside Seal.tsx; the two Seal e2e specs assert only the section heading + encryption round-trip, not the Policies panel or a red unhealthy dot. But the change DOES drop a previously-distinct signal: a non-2xx /health (e.g. 502/503 from a proxy in front of a dead daemon) used to render a RED 'unhealthy' dot + a banner stating 'Encryption requests will fail'; it now renders a YELLOW 'unreachable' dot with softened copy ('may be CORS/network… may still work in-process'). No red 'daemon-down' surface remains. → Confirm the finder's read but split it: the 'Policies' panel removal is harmless cleanup (it was an honest EmptyState placeholder for data SealInfo never carried — no consumer, no test, zero remaining refs), keep it removed. The probe change is a real-but-low-severity fidelity loss worth a human glance: the new /health route is correct (key-server.ts confirms GET /health returns {status:'up'}, strictly better than the old /v1/service which 400s a bare GET), and a non-2xx still raises a warning banner — but it collapsed RED 'unhealthy' into YELLOW 'unreachable' and the new banner copy ('may still work in-process') actively downplays a 502/503 that genuinely means the daemon is down. If a distinct down-daemon indicator matters, restore a red state for the in-cors non-2xx branch (res not ok with a readable status) while keeping the opaque/network failure as yellow 'unreachable'. Otherwise accept as intended simplification.
- **snapshot.captureProgress / snapshot.captureStarted (running-progress UI)** — `` — No production producer exists: src/cli/wirings/up.ts's makeSnapshotCommandHandler emits only snapshot.captured; the supervisor emits only captureFailed/captureSkipped; the snapshot orchestrator's capture() publishes nothing. The ONLY object-literal emitters of captureStarted/captureProgress are (a) the test fixture app-flow.test.tsx and (b) packages/devstack/dist/cli/wirings/up.mjs — a gitignored STALE build artifact compiled from an older up.ts that still had the dropped onProgress producer. The .claude/worktrees/devstack-dashboard hit is a sibling git worktree on a different branch, not this tree. No dynamic/string-template/codegen construction of these tags. Removing them loses no production behavior — the running-progress TUI never renders because nothing fires the events. → Atomic feature-flag deletion, NOT a piecemeal dashboard-only edit. Remove the two EngineEvent variants (captureStarted, captureProgress) plus SnapshotCaptureProgressPhase from substrate/events.ts, and every switch case keyed on them in app.tsx, event-log.ts, plain-renderer.ts, projection/update.ts, event-time.ts, and dashboard.tsx (the 'running' SnapshotStatus variant, snapshotPhaseLabel, snapshotPhaseMeansPaused, the 'starting' phase, and the SnapshotStatusLine 'running' branch). Delete the test fixture's two Queue.offer blocks in app-flow.test.tsx (they assert against the dead UI, not real behavior). Do NOT touch the captured/skipped/failed SnapshotStatus variants or their events — those have live producers. Caveat: deleting dashboard.tsx:140 in isolation breaks SnapshotStatusLine exhaustiveness because app.tsx still sets tag:'running'; the producer-and-consumer pair must go together. Sweep typecheck after, and rebuild to refresh the stale dist artifact.

## Corrections — finder claims REFUTED by verification (do NOT touch; these are LIVE)

- FALSE-POSITIVE DEAD CLAIM — ScopedRefMapKeyMissingError.registryName (scoped-registry/service.ts:208): finder claimed stale-comment, but the comment accurately describes a LIVE default discover-by-shape render path in cascade-formatter.ts:251-277; field is populated in the live get() failure path of package+coin registries and asserted by a unit test. Keep.
- FALSE-POSITIVE DEAD CLAIM — ScopedMultimap.entriesFor: finder claimed single-caller, but it backs BOTH the multimap surface (strategy-registry) AND the single-mode find/get/has (coin+package). 3+ call sites; inlining would duplicate the projection. Keep.
- FALSE-POSITIVE DEAD CLAIM — defineScopedRefMap: NOT a deletable duplicate; it is the live production single-mode factory imported by name from both coin and package registries. Keep (cosmetic rename only, gated on dead-sibling removal).
- FALSE-POSITIVE DEAD CLAIM — causeTagOf (supervisor/acquire-node.ts:222): finder claimed duplicate, but its alleged clone (dispatch-contributions.ts) is already deleted; it is the SOLE live copy feeding dispatchFailed.causeType into 2 operator TUI surfaces. Keep.
- FALSE-POSITIVE DEAD CLAIM — dispatchBufferedContributions pluginRole param: finder claimed over-thread, but role is a genuine data dependency filling the mandatory PluginSpanContext.role attribute; the identity tuple is role-blind by design. Keep.
- FALSE-POSITIVE DEAD CLAIM — SpanAttr.network span attribute (spans.ts:26): finder claimed stale-comment, but identity.chain is a deliberately-retained live correlation field stamped at 4 supervisor callsites and mirrored to the user-visible n= TUI dimension. Keep.
- FALSE-POSITIVE DEAD CLAIM — plan subsume-comment (graph.ts:5): finder's premise is factually false; HEAD comment contains none of the claimed stale text and accurately documents live wrappers (plan/planExcluding/planRestart). No change.
- FALSE-POSITIVE DEAD CLAIM — acquireExclusive/oneShotSweep/timeoutMillis===0 branch (stack-lock.ts): symbols do not exist at HEAD; the proposed cleanup was already committed in 8aba3389. No action.
- FALSE-POSITIVE DEAD CLAIM — formatCause local helper: finder labeled 'duplicate/dead' but it IS called at acquire-node.ts:313; it can be DEDUPED (repoint to formatValue) but not deleted outright. (Captured as a dedup action, not a raw delete.)
- FALSE-POSITIVE DEAD CLAIM — endpointSinksFromRoute / ManifestEndpointRegistryService / layerManifestEndpointRegistry (boot.ts): finder claimed single-caller/dead; all three are live production code on the runStack contribution-dispatch path. Keep.
- FALSE-POSITIVE DEAD CLAIM — assertNoDanglingRefs + walkFiles (create-devstack-app/compose.ts): finder claimed dead/duplicate, but the guard runs on every live scaffold and has an asserting test; walkFiles backs it. Keep (NEEDS-HUMAN for the guard; walk dedup is a refactor, not a delete).
- FALSE-POSITIVE DEAD CLAIM — vault.move template clone: finder claimed duplicate; it is a deliberate cross-publish-boundary clone published to npm and consumed by every scaffolded seal app. Deleting breaks scaffolds. Keep.
- FALSE-POSITIVE DEAD CLAIM — examples/deepbook-trader/move/vendor: finder claimed duplicate; deleting breaks TWO live config evaluations (devstack.config.ts localPackage + snapshot-matrix stack.ts). Keep.
- FALSE-POSITIVE DEAD CLAIM — makeCoinSnapshotable (coin/snapshot.ts): finder claimed stale-comment/dead, but its preRestore payload is read by the snapshot orchestrator's warm-restart identity guard. Keep.
- FALSE-POSITIVE DEAD CLAIM — PackageRegistry (interface): finder labeled over-general/removable, but it is the live service shape (params + yields). Only the wrapRefMap indirection + has/changes members are prunable; the type stays.
- FALSE-POSITIVE DEAD CLAIM — fetchRecentBlobs / fetchShardAssignments (dashboard walrus.ts): finder claimed dead, but each is the queryFn of a live React-query hook rendered in Walrus.tsx. Only the export keyword is residue (un-export, don't delete).
- FALSE-POSITIVE DEAD CLAIM — FundingEntry / signAndDispatch generics / DEFAULT_SUI_CLI_VERSION+suiCliImageBuildContext / containerImagePath-adjacent suiCli / OptionalService.readEffect / projectionLevel / dispatchBufferedContributions: each flagged dead/single-caller/over-general but verified live with real production consumers or load-bearing type roles. Keep.
- FALSE-POSITIVE DEAD CLAIM — codegenable / StrategyFor (knip sweep) / scoped-registry ScopedRefMapKeyMissingError barrel line / define-capabilities.ts filename: codegenable is live public API; substrate StrategyFor<K> is structurally referenced in the public PluginCtx (only inlinable, not deletable); ScopedRefMapKeyMissingError barrel re-export IS test-consumed (must stay while the 4 siblings go); define-capabilities.ts is not removable (live barrel exports) — at most a human rename. Keep / partial-only.
- FALSE-POSITIVE DEAD CLAIM — many over-exported consts flagged with nonzero estLocSaved (FORK_IMAGE_ENV_VAR, DEFAULT_*_READY_TIMEOUT, FORK_VALIDATOR_STOP_GRACE_SECONDS, SEAL_KEYGEN_*, CONTAINER_SCRUB_AWK_SCRIPT, tar reader helpers, HealthState/HealthPolicy/StackViewModel/DashboardSummary/DisplayCells/AccountCells/PackageCells, deepbookMoveAssetRoot/deepbookMoveSource): the BODIES are live; only the export keyword is removable (net ~0 LOC). The finder's LOC estimates assumed deletion and over-count.
- FALSE-POSITIVE DEAD CLAIM — clearStaleRestoreMarker / RESTORE_PENDING_FILE_NAME / restore-marker-tolerance.ts: the source module is already deleted at HEAD (8aba3389). The only residue is a stale gitignored dist artifact (git rm or rebuild). No source action.
- FALSE-POSITIVE DEAD CLAIM — CapabilitySinks design-rationale comments (scoped-registry) + supervisor 'CapabilitySinks loop' provenance comments: the specific stale text the finder targeted does NOT exist at HEAD (already scrubbed). No-op; do NOT prune the strategy-bus-guard.test.ts:10 rationale that documents why the guard exists.
- SCOPE CORRECTION — defineScopedRegistry barrel split (knip): only 4 of the 5 listed re-export lines (defineScopedRegistry, MultimapEntry, MultimapItem, ScopedMultimap) are safe to trim; ScopedRefMapKeyMissingError MUST stay (test imports it from that barrel and it is part of the consumed ScopedRefMap.get error surface).
- SCOPE CORRECTION — README.md deepbook vendored asset: a real ~45-line shipped-bytes win, but the action is an isExcluded() rule in fetch/build scripts + deleting the materialized copies, NOT a TS-LOC edit. Bundle with dusdc bundled-package removal.

## Completeness critic — modalities possibly missed

- Branch-added package.json dependencies/devDependencies that are never imported (dependency hygiene). NONE of the 12 buckets diffed package.json dependency blocks — create-devstack-app and cli-surfaces buckets looked only at TS symbols/scripts, knip explicitly scopes to exports/types/files/duplicates and does NOT flag unused package.json deps. This branch added several deps and at least two are dead.
- Orphaned devDependencies left behind after a script was deleted (tool-removal residue). When a build/check script is deleted, its dedicated dev tool (runner/linter) is often left in devDependencies. The create-devstack-app bucket noted the sync-template.ts script deletion but did not check whether its runner tooling survived as a dead devDep.
- npm 'files'-array vs .gitignore consistency for generated artifacts (verified CLEAN here, but unchecked by buckets): the deepbook bucket audited the fetch/build scripts and the example-vendor duplication but never confirmed whether the newly-shipped move-assets/ and fetched deepbookv3/ trees are gitignored vs accidentally committed. They ARE correctly gitignored (packages/devstack/.gitignore:10,16) — modality is clean but was a genuine blind spot.
- Committed .devstack generated-extras consumed via new tsconfig path alias (verified clean): the cli/tsconfig buckets saw the new @devstack-dev/* alias + tsconfig include of .devstack/stacks/*/generated-extras but did not verify those generated trees are not committed (they are not tracked — git ls-files empty).

### Critic — additional candidates

- **@mysten/walrus-wasm devDependency (^0.2.0)** — `packages/devstack/package.json:101` — DEAD devDependency added by this branch. grep for 'walrus-wasm' across packages/devstack/{src,test,scripts} returns ZERO matches. The wasm binding is only consumed via Vite '?url' imports in BROWSER app code (examples/private-content/src/lib/walrus.ts, create-devstack-app/template/src/lib/walrus.ts) — the devstack package itself has no browser bundle and no ?url wasm import. Its e2e (test/e2e/snapshot-matrix/clients.ts) imports only '@mysten/walrus' (the JS SDK), never walrus-wasm. @mysten/walrus does not declare walrus-wasm as a peer that would force its install. Sibling @mysten/walrus (^1.1.7, line 100) and @mysten/seal (line 99) ARE legitimately used by the snapshot-matrix e2e, so only walrus-wasm is the dead one. Low risk to delete.
- **tsx devDependency (catalog: -> ^4.21.0)** — `packages/create-devstack-app/package.json:54` — Orphaned devDependency. The branch DELETED scripts/sync-template.ts and its two npm scripts ('check-template'/'sync-template' which ran `node --experimental-strip-types scripts/sync-template.ts`). After deletion the remaining scripts are build=tsdown, test=vitest run, typecheck=tsc — none invoke tsx. grep for 'tsx' in package.json/scripts/src finds only the file-extension regex strings in compose.ts (.tsx? pattern) and panel .tsx filenames, never the tsx RUNNER. The deleted sync-template path was the only plausible tsx consumer; the dep is now dead. (Note: tsx was added in the same diff that removed sync-template's node --experimental-strip-types invocation, so it appears to be a never-wired replacement.)
- **@mysten/walrus pinned ^1.1.7 (not catalog:)** — `packages/devstack/package.json:100` — Lower-value dependency-hygiene smell adjacent to the above: @mysten/walrus and @mysten/walrus-wasm are hard-pinned to ^1.1.7 / ^0.2.0 while every other @mysten/* dep in this package (sui, seal, deepbook-v3, codegen) uses catalog:. This bypasses the workspace version catalog (pnpm-workspace.yaml) for two of the three new deps, risking version drift from the rest of the monorepo. If @mysten/walrus is kept, it should move to catalog: for consistency; walrus-wasm should be deleted outright per the candidate above.