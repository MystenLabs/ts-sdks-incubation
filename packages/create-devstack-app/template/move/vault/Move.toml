[package]
name = "vault"
edition = "2024"

# Sui, MoveStdlib, Bridge, DeepBook, SuiSystem are auto-added by sui-cli
# (the 2024 edition behavior). No explicit deps needed.
[dependencies]

[addresses]
vault = "0x0"
