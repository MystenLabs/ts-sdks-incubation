---
title: Coins and Funding
description: Current coin declaration and funding forms.
---

The `coin` namespace has explicit forms. There is no string-guessing `coin(value)` entry.

```ts
import { defineDevstack, account, coin, localPackage } from '@mysten-incubation/devstack';

const publisher = account('publisher');

const managed = localPackage('managed_coin', {
	sourcePath: './move/managed_coin',
	publisher,
});

const suiCoin = coin.builtin('sui');
const managedCoin = coin.fromPackage(managed, 'MANAGED_COIN');

const alice = account('alice', {
	funding: [
		{ coin: suiCoin, amount: 1_000_000_000n },
		{ coin: managedCoin, amount: 1_000n },
	],
});

export default defineDevstack({ members: [publisher, managed, suiCoin, managedCoin, alice] });
```

Available forms:

- `coin.builtin('sui')` uses the protocol SUI type.
- `coin.fromPackage(member, witness)` adds a dependency edge on the publishing member.
- `coin.local(symbol)` resolves by symbol from the per-stack registry. Compose the publishing member
  before the consumer, because this form does not carry a direct edge.
- `coin.known(fullCoinType)` resolves an on-chain type through metadata reads.

Funding is best-effort for custom strategies. Missing strategies are treated as a no-op for
cross-coin funding entries, while default ephemeral SUI funding fails loudly if no faucet-bearing
strategy exists for the active chain.
