---
title: wallet
description:
  Multi-coin balances + send/receive, DeepBook v3 swap UI against locally-published pools.
---

Multi-coin balance UI plus a DeepBook v3 swap interface against locally-published pools. Shows how
`importMovePackage` pulls upstream Move packages (DeepBook) into the local stack so the frontend
trades against real pool objects.

Source:
[`examples/wallet/`](https://github.com/mysten-incubation/devstack/tree/main/examples/wallet)
