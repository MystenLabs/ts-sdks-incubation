---
'@mysten-incubation/devstack-wallet-panels': minor
---

Initial release. Lit panels (Faucet, Packages, Network) for the
`@mysten-incubation/dev-wallet` panel API, consuming
`virtual:devstack-manifest` from `@mysten-incubation/devstack`. Drop in
via `devstackPanels()` after `configureDevstackPanels(manifest)`.
